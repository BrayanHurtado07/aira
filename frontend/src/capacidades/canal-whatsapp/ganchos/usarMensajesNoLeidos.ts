// Placeholder — en producción consultaría un endpoint específico
// como GET /mensajes/no-leidos para obtener el conteo real por empresa
export function usarMensajesNoLeidos() {
  return {
    total: 0,
  }
}
